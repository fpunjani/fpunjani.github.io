# Triggered once to add two links to the Bawa random-sites button.
name: Add Bawa links once

on:
  push:
    paths:
      - '.github/workflows/add-bawa-links-once.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-index:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Add requested links and remove this workflow
        run: |
          python - <<'PY'
          from pathlib import Path

          path = Path('index.html')
          text = path.read_text(encoding='utf-8')

          old = """            'https://www.bbc.com/future',
            'https://pudding.cool'
"""
          new = """            'https://www.bbc.com/future',
            'https://pudding.cool',
            'https://www.ippractice.ca/patent-and-trademark-statistics/expiring-patents/',
            'https://www.galactanet.com/writing.html'
"""

          if 'https://www.ippractice.ca/patent-and-trademark-statistics/expiring-patents/' not in text:
              if old not in text:
                  raise SystemExit('Expected randomSites block not found')
              text = text.replace(old, new, 1)
              path.write_text(text, encoding='utf-8')
          PY
          rm -f .github/workflows/add-bawa-links-once.yml
      - name: Commit changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html .github/workflows/add-bawa-links-once.yml
          git diff --cached --quiet || git commit -m "Add Bawa random links"
          git push
